<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-fluid">
		<a class="navbar-brand" href="/">Ikeo</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor03">
			<ul class="navbar-nav me-auto">
				{% for c in categoryRepository.findAll() %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('product_category', {'slug': c.slug}) }}">{{ c.name }}</a>
					</li>
				{% endfor %}
			</ul>
			<ul class="navbar-nav">
				<li class="nav-item">
					<a href="{{ path('cart_show') }}" class="btn btn-dark">
						<i class="fas fa-shopping-cart"></i>
						Panier
						{{ cartService.total }}
						&euro;</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a href="{{ path('admin_index') }}" class="btn btn-warning">
							Accès Administrateur</a>
					</li>
				{% elseif app.user %}
					<li class="nav-item">
						<a href="{{ path('app_logout') }}" class="btn btn-danger">
							Se déconnecter</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a href="" class="btn btn-info">
							S'inscrire</a>
					</li>
					<li class="nav-item ml-3">
						<a href="{{ path('app_login') }}" class="btn btn-success">
							Se connecter</a>
					</li>
				{% endif %}

			</ul>
		</div>
	</div>
</nav>
